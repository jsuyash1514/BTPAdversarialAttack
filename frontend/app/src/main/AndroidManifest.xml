<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.btpfrontend">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/btplogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/btplogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.BTPfrontend"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.ImageOutputActivity"></activity>
        <activity android:name=".ui.ImageInputActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>